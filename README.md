# CSVReader

```cpp
#include "CSVReader.hpp"

int main(){
/*
* CSVReader reader({path here},{separate symbol:options,default:','});
*
* */



    /*/////////////////////////////////////////////
     * ReadTable and print both can only use once in an initial
     * Can't mix readTable and readLine function besides reinitialize
     * */

    /*  Read a whole table return as a vector<vector<string>>
     *
     * reader.readTable()
     * */


    /* Read as line
     *
     * reader.readLine()
     * reader.hasNextLine
     * */


    /* Immediate print the csv file
     *
     * reader.print()
     * */

    return 0;
}
```
